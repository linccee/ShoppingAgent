# 镜澜导购 (Sales Agent) - 项目介绍

## 1. 项目概述

**镜澜导购**是一款多平台AI购物助手，帮助用户在 Google Shopping、Amazon 和 eBay 三大平台比较商品价格、汇总_reviews、分析情感、换算货币，并生成购买建议。该应用具有跨会话的持久化对话记忆功能。

### 核心功能

| 功能 | 描述 |
|------|------|
| 多平台搜索 | 同时搜索 Google Shopping、Amazon、eBay 商品 |
| 价格比较 | 实时获取各平台最新价格 |
|  Reviews分析 | 提取_reviews要点，分析情感倾向 |
| 货币换算 | 支持 USD、EUR、GBP、JPY、CNY、KRW 等主流货币 |
| AI推荐 | 基于价格、_reviews、平台信誉生成购买建议 |
| 会话记忆 | 支持多轮对话，上下文持久化到MongoDB |

---

## 2. 技术栈

### 前端技术栈

| 类别 | 技术 | 版本 |
|------|------|------|
| 框架 | React | 18 |
| 语言 | TypeScript | 5 |
| 构建工具 | Vite | 6 |
| 样式 | TailwindCSS | 3 |
| 路由 | React Router DOM | 7 |
| 状态管理 | React Context | - |
| HTTP客户端 | Axios | - |
| 国际化 | i18next | zh-CN/en-US/ja-JP/ko-KR |
| Markdown渲染 | react-markdown + remark-gfm | - |
| 图标 | lucide-react | - |
| 测试 | Vitest | - |
| 代码检查 | ESLint | - |

### 后端技术栈

| 类别 | 技术 | 版本 |
|------|------|------|
| 框架 | FastAPI | 0.116+ |
| 运行时 | Uvicorn | - |
| AI Agent框架 | LangChain + LangGraph | - |
| LLM | OpenAI兼容API (默认Qwen) | - |
| 数据库 | MongoDB | 4.15+ |
| Checkpoint存储 | langgraph-checkpoint-mongodb | - |
| 认证 | PyJWT + bcrypt | - |
| 搜索API | SerpAPI + Tavily | - |
| 异步任务 | SSE (Server-Sent Events) | - |
| 分词 | tiktoken | - |
| 测试 | pytest | - |

---

## 3. 项目目录结构

```
1-agent/
├── backend/                    # FastAPI 后端应用
│   ├── app/                   # FastAPI 核心应用
│   │   ├── api/routes/        # API 路由 (auth, chat, health, session, users)
│   │   ├── core/              # 安全与JWT工具
│   │   ├── models/            # Pydantic 请求/响应模型
│   │   ├── services/          # 业务逻辑服务
│   │   ├── utils/             # 日志配置
│   │   └── main.py            # FastAPI 入口
│   ├── agent/                 # LangGraph ReAct Agent 核心
│   │   ├── agent_core.py      # Agent 创建、流式执行、回调（生产入口）
│   │   ├── factory.py         # 基于 Registry 的 ReAct Agent 工厂
│   │   ├── registry.py        # Agent 类型与工具注册表
│   │   ├── prompt.py          # 系统提示词
│   │   ├── memory_manager.py  # 记忆摘要压缩与序列校验
│   │   ├── compressed_checkpointer.py  # MongoDB 双轨持久化 checkpointer
│   │   ├── compression_retry.py        # 压缩失败重试与降级
│   │   └── tool_output_compressor.py   # 工具输出裁剪
│   ├── tools/                 # Agent工具
│   │   ├── search_tool.py     # 多平台商品搜索
│   │   ├── price_tool.py      # 价格提取
│   │   ├── review_tool.py     # Reviews情感分析
│   │   ├── currency_exchange_tool.py  # 货币转换
│   │   └── tavily_tool.py     # 实时网络搜索
│   ├── utils/
│   │   └── db.py              # MongoDB连接与会话管理
│   └── requirements.txt       # Python依赖
│
├── frontend/                  # React TypeScript 应用
│   ├── src/
│   │   ├── pages/             # Login, Register, Profile 页面
│   │   ├── components/        # React 组件
│   │   │   ├── Chat/          # 聊天相关组件
│   │   │   ├── Hero/          # 落地页区域
│   │   │   ├── Sidebar/       # 侧边栏导航
│   │   │   └── common/        # 通用组件
│   │   ├── context/           # React Context 状态管理
│   │   ├── hooks/             # 自定义Hooks
│   │   ├── i18n/              # 国际化配置
│   │   ├── services/          # API客户端、SSE处理
│   │   ├── api/               # API端点封装
│   │   ├── styles/            # 全局样式
│   │   └── types/             # TypeScript类型定义
│   └── package.json
│
├── test/                      # pytest 测试套件
├── docs/                      # 文档和图片
├── scripts/                   # 工具脚本
├── tools/                     # 独立Python工具
├── logs/                      # 日志文件

```

---

## 4. 前端模块详解

### 4.1 页面路由

| 路由 | 组件 | 说明 |
|------|------|------|
| `/login` | Login.tsx | 用户登录 |
| `/register` | Register.tsx | 用户注册 |
| `/chat` | Shell (App.tsx内) | 主聊天界面 (需登录) |
| `/profile` | Profile.tsx | 用户偏好设置 (需登录) |
| `/` | 重定向 | 根路径重定向到 `/chat` |

**路由保护**: `PrivateRoute.tsx` 封装受保护路由，未登录用户自动跳转 `/login`。

### 4.2 核心组件

#### Chat 模块 (`components/Chat/`)

| 组件 | 职责 |
|------|------|
| `ChatMessage.tsx` | 渲染单条消息 (用户/AI)，包含头像、Markdown内容、工具时间线 |
| `ChatTimeline.tsx` | 可展开/折叠的时间线，展示工具执行步骤 |
| `ChatInput.tsx` | 文本输入框，包含提示信息和流式控制 (发送/停止) |
| `MarkdownContent.tsx` | 使用react-markdown渲染Markdown内容 |

#### Sidebar 模块 (`components/Sidebar/`)

| 组件 | 职责 |
|------|------|
| `Sidebar.tsx` | 左侧导航面板，包含品牌区、用户信息、运行时配置、会话统计、新建/清空会话按钮 |
| `SessionList.tsx` | 会话历史列表，支持滚动和删除 |

#### Hero 模块 (`components/Hero/`)

| 组件 | 职责 |
|------|------|
| `Hero.tsx` | 欢迎横幅，无消息时展示示例提示词 |

#### 通用组件 (`components/common/`)

| 组件 | 职责 |
|------|------|
| `Button.tsx` | 可复用按钮，支持 primary/secondary/ghost/danger 变体 |
| `Toast.tsx` | 通知系统，支持 success/error/warning/info 类型 |
| `ConfirmDialog.tsx` | 模态框确认对话框 |
| `LanguageSwitcher.tsx` | 语言选择器 |
| `Loading.tsx` | 加载动画 |

### 4.3 状态管理

**Context 层** (`context/`):

| 文件 | 职责 |
|------|------|
| `AppContext.tsx` / `AppContexts.ts` | 全局应用状态 Context |
| `appReducer.ts` | 类Redux的Reducer，处理状态更新逻辑 |
| `useAppStore.ts` | 提供 `useAppState` 和 `useAppDispatch` Hooks |
| `AuthContext.tsx` | 认证状态 (用户、令牌、登录状态) 和认证操作 |
| `LanguageContext.tsx` | 国际化语言偏好管理 |

**Reducer处理的关键事件**:
- Bootstrap (加载/错误状态)
- Health/runtime配置更新
- 会话CRUD操作
- 流式事件 (开始、token、工具调用、停止、错误、完成)

### 4.4 自定义Hooks

| Hook | 职责 |
|------|------|
| `useChat.ts` | 管理消息发送和停止生成，向Reducer分发流式事件 |
| `useSessions.ts` | 会话生命周期管理 (创建、加载、删除、初始化) |

### 4.5 前端-后端交互

**API层**:

| 文件 | 职责 |
|------|------|
| `api/client.ts` | Axios客户端，配置拦截器处理认证令牌和401响应 |
| `api/auth.ts` | 登录、注册、刷新、登出 API |
| `api/user.ts` | 用户信息、偏好设置 API |
| `services/api.ts` | 基于fetch的REST API (健康检查、会话CRUD) |
| `services/sse.ts` | SSE流式聊天处理，解析事件类型 |

**SSE事件类型**:

| 事件 | 说明 |
|------|------|
| `session` | 会话ID |
| `token` | AI生成的token |
| `tool_start` | 工具开始执行 |
| `tool_end` | 工具执行结束 |
| `token_usage` | Token使用统计 |
| `stopped` | 生成停止 |
| `error` | 错误发生 |
| `done` | 完成 |

---

## 5. 后端模块详解

### 5.1 API 端点

**基础路径**: `/api/v1`

| 路由 | 前缀 | 端点 |
|------|------|------|
| health | `/api/v1` | `GET /health` |
| auth | `/api/v1/auth` | `POST /register`, `POST /login`, `POST /refresh`, `POST /logout` |
| users | `/api/v1/users` | `GET /me`, `PUT /me`, `PUT /me/preferences`, `DELETE /me` |
| session | `/api/v1/sessions` | `POST /`, `GET /`, `GET /{session_id}`, `DELETE /{session_id}` |
| chat | `/api/v1/chat` | `POST /stream`, `POST /stop` |

### 5.2 服务层

| 服务 | 职责 |
|------|------|
| **AuthService** | 用户注册 (密码强度验证)、登录认证、JWT令牌发放 |
| **UserService** | 用户资料CRUD、偏好设置管理 |
| **SessionService** | 会话CRUD，带用户隔离 (用户只能访问自己的会话) |
| **AgentService** | 封装购物Agent，管理SSE流式输出，停止信号处理 |

### 5.3 数据模型

**用户相关模型** (`models/user.py`):

| 模型 | 用途 |
|------|------|
| `UserCreate` | 注册请求 (username, email, password, browser_language) |
| `UserLogin` | 登录请求 |
| `UserPreferences` | 用户设置 (currency, platforms, budget_range, notification_enabled, language_preference) |
| `UserResponse` / `UserInDB` | 用户数据传输对象 |
| `TokenResponse` / `RegisterResponse` | 认证响应 |

**请求/响应模型** (`models/request.py`, `models/response.py`):

| 模型 | 用途 |
|------|------|
| `ChatStreamRequest` | 流式聊天请求 (message, session_id) |
| `SessionCreateResponse` | 会话创建响应 |
| `SessionSummaryResponse` | 会话摘要 |
| `SessionDetailResponse` | 会话详情 |
| `HealthResponse` | 健康检查响应 |

### 5.4 依赖注入

- `app/api/dependencies.py`: 提供 `get_session_service()` 和 `get_agent_service()` 单例缓存
- `app/core/deps.py`: `get_current_user()` 依赖，解码JWT Bearer令牌并查询数据库

### 5.5 核心基础设施

**安全模块** (`app/core/security.py`):
- 密码加密: `bcrypt` (rounds=12)
- JWT令牌: `PyJWT` (HS256算法)
- 令牌声明: `sub`(user_id), `username`, `iat`, `exp`

**配置模块** (`app/config.py`):
- 环境变量配置 via `python-dotenv`
- CORS跨域配置
- Agent参数: `MAX_ITERATIONS=150`, `TEMPERATURE=0.3`, `MAX_TOKENS=2048`, `MEMORY_TURNS=10`
- 记忆压缩: `ENABLE_MEMORY_COMPRESSION`, `COMPRESSION_THRESHOLD=1500`

**日志模块** (`app/utils/logging_config.py`):
- 按小时轮转的文件日志 `logs/{module}/{date}/{module}_{HH}h.log`
- 模块隔离: auth, chat, session, user, tools, db, agent, api
- 保留168个轮转文件 (7天)

---

## 6. LangGraph Agent 模块详解

### 6.1 架构：ReAct Agent

项目使用 LangGraph 预置的 **ReAct Agent**（`create_react_agent`），由 LLM 根据 `SYSTEM_PROMPT` 自主决定工具调用顺序，而非固定 StateGraph 节点编排。

**执行链路**:
```
POST /api/v1/chat/stream
  → AgentService.stream()
  → agent_core.create_shopping_agent() + stream_agent()
  → LangGraph ReAct Agent（tools + CompressedCheckpointer）
```

**注册工具**: `search_products`、`prices`、`analyze_reviews`、`currency_exchange`、`tavily_search`、`tavily_extract`

**会话隔离**: 每次调用传入 `config={"configurable": {"thread_id": session_id}}`，不同会话记忆互不干扰。

### 6.2 典型购物流程（Agent 动态决策）

以下流程由 ReAct Agent 根据用户意图动态执行，并非硬编码节点图：

```
用户输入 → search_products → ┬→ prices ────────┐
                             ├→ analyze_reviews ┼→ LLM 汇总推荐
                             └→ currency_exchange┘
```

### 6.3 记忆机制

| 组件 | 职责 |
|------|------|
| `CompressedCheckpointer` | 包装 `MongoDBSaver`，原始 checkpoint 立即落库，压缩态异步持久化 |
| `memory_manager.py` | 按完整对话轮次做 LLM 摘要，保留近期 token budget，校验 tool call 序列 |
| `compression_retry.py` | 压缩失败指数退避重试，超限后降级（提高阈值 / 暂时跳过压缩） |
| `tool_output_compressor.py` | 持久化前裁剪工具返回的冗余字段 |

**MongoDB 集合**:
- LangGraph checkpoint（原始消息历史）
- `compressed_agent_states`（压缩后的 LLM 上下文）
- `sessions`（前端展示用聊天记录，与 Agent 推理上下文分离）

### 6.4 Agent 创建

**生产入口 — `agent_core.py`**:
- `create_shopping_agent()` 创建 ReAct Agent，注入 `CompressedCheckpointer`
- `stream_agent()` 在后台线程运行，通过队列 yield token / 工具事件
- `AgentService` 直接 import 此模块，不经过 factory

**可选入口 — `factory.py`**:
- 通过 `AgentRegistry` 按 Agent 类型配置 LLM 参数与工具集
- `create_shopping_agent()` 同样返回 ReAct Agent（Registry 驱动版本）

---

## 7. 工具模块详解

### 7.1 搜索工具 (`tools/search_tool.py`)

**功能**: 多平台商品搜索

| 平台 | 引擎 | SKU来源 |
|------|------|---------|
| Google Shopping | `google` (tbm=shop) | - |
| Amazon | `amazon_product` | ASIN |
| eBay | `ebay_product` | product_id |

**搜索规则**:
- 使用英文关键词搜索
- 不添加日期/年份限制

### 7.2 价格工具 (`tools/price_tool.py`)

**功能**: 获取商品实时价格

| 平台 | 数据来源 |
|------|----------|
| Amazon | `product_results.price` |
| eBay | `buy.buy_it_now.price` |

### 7.3 Reviews工具 (`tools/review_tool.py`)

**功能**: 情感分析和Reviews提取

**情感判断**:
- 评分 ≥ 4.0 → 正面
- 评分 ≤ 2.5 → 负面
- 其他 → 中性

**数据提取**:
- Amazon: rating, reviews数量, summary, 评论列表
- eBay: reviews数量, 评论列表 (最多10条)

### 7.4 货币转换工具 (`tools/currency_exchange_tool.py`)

**API**: exchangerate-api.com (v6)

**支持的货币**: USD, EUR, GBP, JPY, CNY, AUD, CAD, INR, KRW

**货币检测** (从用户查询):
- `¥` / `人民币` / `元` / `CNY` → CNY
- `£` / `英镑` → GBP
- `€` / `欧元` → EUR
- `$` / `美元` → USD

**预算提取**: 支持 `¥3000`, `$500`, `预算: 3000` 等格式

**默认假设**: 中文 → CNY，英文 → USD

### 7.5 Tavily工具 (`tools/tavily_tool.py`)

**功能**: 实时网络搜索和URL内容提取

| 工具 | 用途 |
|------|------|
| `tavily_search` | 获取最新/时效性信息 |
| `tavily_extract` | 从URL提取内容 |

### 7.6 工具并行规则

| 工具 | 规则 |
|------|------|
| `search_products` | 必须串行，每步一次调用 |
| `prices` | 可并行，同工具最多3个并发 |
| `analyze_reviews` | 可并行 |
| `currency_exchange` | 可并行 |

---

## 8. 数据库设计

### 8.1 MongoDB 集合

| 集合名 | 用途 |
|--------|------|
| `users` | 用户账户信息 |
| `sessions` | 聊天会话 (包含消息、token使用、用户ID) |
| `compressed_agent_states` | 压缩后的LLM状态 (用于记忆优化) |
| `compression_failed_tasks` | 压缩失败任务 (用于重试) |
| `compression_degradation_state` | 降级追踪 (按线程) |

### 8.2 检查点机制

- 使用 `langgraph.checkpoint.mongodb` 的 `MongoDBSaver`
- 被 `CompressedCheckpointer` 封装以支持状态压缩
- 会话隔离通过 `thread_id` = `session_id` 实现

---

## 9. 认证流程

### 9.1 注册流程

```
用户提交注册 → 密码强度验证 → bcrypt加密 → 存入MongoDB → 返回成功响应
```

### 9.2 登录流程

```
用户提交登录 → 验证密码 → 生成JWT令牌 → 返回access_token和用户信息
```

### 9.3 JWT令牌结构

| 声明 | 内容 |
|------|------|
| `sub` | user_id |
| `username` | 用户名 |
| `iat` | 签发时间 |
| `exp` | 过期时间 |

### 9.4 前端认证状态

- Token存储在 `localStorage` (`access_token`, `user`)
- `AuthContext` 管理认证状态
- 401响应自动清除存储并触发登出
---

## 10. 日志系统

### 10.1 日志目录结构

```
logs/
├── auth/          # 认证模块日志
├── chat/          # 聊天模块日志
├── session/       # 会话模块日志
├── user/          # 用户模块日志
├── tools/         # 工具模块日志
├── db/            # 数据库日志
├── agent/         # Agent日志
└── api/           # API日志
```

### 10.2 日志格式

- 轮转周期: 每小时
- 保留时间: 7天 (168个文件)
- 文件格式: `{module}_{HH}h.log` (如 `chat_14h.log`)

---

## 11. 国际化

### 11.1 支持的语言

| 语言代码 | 语言 |
|----------|------|
| `zh-CN` | 简体中文 |
| `en-US` | 英语 |
| `ja-JP` | 日语 |
| `ko-KR` | 韩语 |

### 11.2 翻译命名空间

`common`, `auth`, `chat`, `sidebar`, `profile`, `errors`

### 11.3 语言检测

- 使用 `i18next-browser-languagedetector`
- 语言偏好持久化到 `localStorage` (`language_preference`)

---

## 12. API响应格式

所有API响应使用统一的信封格式:

```json
{
  "success": true,
  "data": { ... },
  "error": null,
  "metadata": { ... }
}
```

---

## 13. 开发与部署

### 13.1 启动命令

**后端**:
```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**前端**:
```bash
cd frontend
npm install
npm run dev
```

### 13.2 前端依赖安装

```bash
npm install
```